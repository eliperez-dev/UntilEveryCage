{
  "contract_id": "uec-v2-backend-contract-freeze-1",
  "status": "frozen-for-frontend-integration",
  "as_of": "2026-09-22",
  "public_location_contract": "docs/api/v2-contract.json",
  "public_location_schema": "docs/api/v2-location.schema.json",
  "error_envelope": {
    "shape": {"api_version": "v2", "error": {"code": "string", "message": "string"}},
    "unknown_codes": "clients must handle as generic failures"
  },
  "public_location_pagination": {
    "default_limit": 100,
    "max_limit": 1000,
    "cursor": "preferred deterministic facility_id continuation",
    "offset": "bounded compatibility parameter",
    "mutually_exclusive": ["cursor", "offset"]
  },
  "private_graph": {
    "api_version": "private-graph-v2",
    "page_max": 100,
    "storage_cap": null,
    "connection_types": ["exact", "inferred"],
    "required_metadata": ["source_qualified_endpoints", "confidence", "provenance", "ruleset", "disclaimer"],
    "filters": ["connection_type", "min_confidence", "include_conflicting", "suppressed", "source_id", "entity_id", "cursor"],
    "public_projection": false,
    "inferred_confidence_kind": "ruleset_estimate_not_probability",
    "human_confirmed_state": false
  },
  "public_graph": {
    "api_version": "v2-graph-v1",
    "contract": "docs/api/public-graph-contract.md",
    "page_max": 100,
    "release_gated": true,
    "read_only": true,
    "confidence_bands": ["exact", "high", "medium", "low"],
    "public_projection": true,
    "private_evidence_fallback": false
  },
  "invariants": [
    "Private graph edges never merge universal identities.",
    "Private graph edges never transfer claims.",
    "Private graph edges never authorize publication.",
    "Current suppression applies to reads, maps, exports, history, reimports, and restores.",
    "Public relationship projection is available only through the separately approved release-scoped contract and remains empty until a promoted eligible release exists.",
    "APHIS-to-FSIS automatic links are not generated.",
    "Release/profile, source rights, privacy, factual review, project approval, promotion, and publication are independent gates."
  ],
  "change_policy": "Additive changes require contract/schema tests; breaking changes require a new contract_id and migration path."
}
