import {describe,expect,it} from 'vitest'; import {locations} from '../../src/fixtures/locations'; import {makeExportModel} from '../../src/export/exportModel'; import {previewExport} from '../../src/export/previewExport';
describe('export preview',()=>it('keeps profile and provenance context attached',()=>{const result=JSON.parse(previewExport(makeExportModel(locations,'community')));expect(result.context.profile).toBe('community');expect(result.context.limitations).toContain('Synthetic fixture only');expect(result.rows[0]).toMatchObject({source:'Synthetic register example'});}));
